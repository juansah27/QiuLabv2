/**
 * Fungsi kosong untuk kompatibilitas dengan kode yang masih merujuk ke chartTheme
 * Tidak perlu lagi karena komponen chart telah dihapus dari aplikasi
 */
export const updateChartsOnThemeChange = () => {
  // Fungsi kosong untuk mencegah error
  // Chart telah dihapus dari aplikasi
}; 